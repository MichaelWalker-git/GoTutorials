package main

import "fmt"

func main() {
	fmt.Println("Hi there")
}
// package main- defines a package that can be compiled then executed, must have a fun called main
//package calc- can be used as a dependency - helper code

//package uploader - can be used as a dependency